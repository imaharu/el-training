= @task.name
= form_with model: @task, local:true do |f|
  = f.label :name
  = f.text_field :name
  = f.label :context ,"タスク内容"
  = f.text_field :context
  = f.label :closed_on
  = f.date_field :closed_on, value: Time.now.strftime("%Y-%m-%d")
  = f.submit
